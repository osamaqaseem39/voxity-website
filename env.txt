# Voxity Environment Variables Template
# Copy these variables to your .env file and fill in your actual values

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://voxity.com

# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/voxity?schema=public"

# SMTP Email Configuration
# Username: info@voxity.io
# Password: Use the email account's password
# Incoming Server: mail.voxity.io
# IMAP Port: 993
# POP3 Port: 995
# Outgoing Server: mail.voxity.io
# SMTP Port: 465

SMTP_USERNAME=info@voxity.io
SMTP_PASSWORD=your_email_password_here
SMTP_INCOMING_SERVER=mail.voxity.io
SMTP_IMAP_PORT=993
SMTP_POP3_PORT=995
SMTP_OUTGOING_SERVER=mail.voxity.io
SMTP_PORT=465

# NextAuth Configuration (if using)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

